# DeepClaude 项目整体架构指南

## 1. 项目概述

DeepClaude 是一个智能、可配置的 AI 模型代理服务。其核心设计思想是通过一个“推理模型”（Reasoner Model，如 DeepSeek）来分析和增强用户请求，然后将增强后的请求发送给一个“目标模型”（Target Model，如 Claude 或任何与 OpenAI 兼容的模型），从而获得更高质量、更具深度的回答。

整个服务被封装在一个与 OpenAI API 兼容的 FastAPI 后端中，并提供一个简洁的前端页面用于动态配置和管理模型、代理和系统设置。

**核心特点:**

*   **模型组合**：通过“推理+目标”的双模型链式调用，提升回答质量。
*   **高度可配置**：支持通过 UI 或 API 动态添加、修改和管理多种模型（推理模型、目标模型、组合模型）。
*   **OpenAI 兼容**：提供与 OpenAI `v1/chat/completions` 和 `v1/models` 完全兼容的 API 端点，可无缝接入现有生态。
*   **流式与非流式输出**：同时支持流式和非流式响应，满足不同场景需求。
*   **身份验证**：通过 API Key 保护服务接口。
*   **容器化部署**：提供 Docker 和 Docker Compose 配置，简化部署流程。

## 2. 技术栈

*   **后端**:
    *   **框架**: FastAPI
    *   **语言**: Python 3
    *   **HTTP 客户端**: `aiohttp` (用于异步 API 调用)
    *   **依赖管理**: `uv`
*   **前端**:
    *   **框架**: 无 (使用原生 Vanilla JavaScript)
    *   **UI 库**: Bootstrap 5
    *   **语言**: HTML, CSS, JavaScript
*   **部署**:
    *   **容器化**: Docker, Docker Compose

## 3. 架构图

```mermaid
graph TD
    subgraph "用户/客户端"
        A[客户端应用] -->|OpenAI 兼容 API 请求| B{DeepClaude 服务};
        F[浏览器] -->|访问配置页面| B;
    end

    subgraph "DeepClaude 服务 (FastAPI)"
        B -- "路由到 /v1/chat/completions" --> C[ModelManager];
        B -- "路由到 /v1/config/*" --> C;
        B -- "路由到 /config" --> G[静态文件服务];

        C -- "读取/写入" --> D[model_configs.json];
        C -- "根据模型配置创建实例" --> E1[DeepClaude 实例];
        C -- "或" --> E2[OpenAICompatibleComposite 实例];

        E1 -- "1. 调用推理模型" --> H1[DeepSeekClient];
        E1 -- "2. 调用目标模型" --> I1[ClaudeClient];

        E2 -- "1. 调用推理模型" --> H2[DeepSeekClient];
        E2 -- "2. 调用目标模型" --> I2[OpenAICompatibleClient];

        G -- "提供 HTML/CSS/JS" --> F;
    end

    subgraph "外部 AI 服务"
        H1 & H2 --> J[DeepSeek API];
        I1 --> K[Anthropic API (Claude)];
        I2 --> L[其他 OpenAI 兼容 API];
    end

    subgraph "数据与配置"
        D[(模型配置文件)];
    end

    style A fill:#cde4ff
    style F fill:#cde4ff
    style J fill:#d5e8d4
    style K fill:#d5e8d4
    style L fill:#d5e8d4
```

**数据流说明:**

1.  **聊天请求 (`/v1/chat/completions`)**:
    *   客户端应用（如聊天机器人、代码编辑器插件等）向 DeepClaude 发送一个标准的 OpenAI 格式的聊天请求。
    *   FastAPI 应用接收请求，并通过 `verify_api_key` 中间件进行身份验证。
    *   请求被传递给 `ModelManager` 的 `process_request` 方法。
    *   `ModelManager` 根据请求中的 `model` 名称，从 `model_configs.json` 加载对应的“组合模型”配置。
    *   `ModelManager` 确定需要使用的“推理模型”和“目标模型”，并创建或获取相应的模型实例（`DeepClaude` 或 `OpenAICompatibleComposite`）。
    *   模型实例首先调用 `DeepSeekClient`，向 DeepSeek API 发送请求以获取“推理内容”。
    *   获取推理内容后，模型实例将其与原始用户输入组合成一个新的提示（Prompt）。
    *   模型实例调用相应的目标客户端（`ClaudeClient` 或 `OpenAICompatibleClient`），将新提示发送给目标 AI 服务（如 Claude API）。
    *   目标 AI 服务的响应（流式或非流式）被格式化为 OpenAI 兼容的格式，并返回给客户端。

2.  **配置管理 (`/config`, `/v1/config/*`)**:
    *   用户通过浏览器访问 `/config` 页面。
    *   FastAPI 提供前端静态文件 (`index.html`, `auth.js`, `config.js` 等)。
    *   前端 `auth.js` 负责处理 API Key 的输入和验证，验证通过后将 Key 存储在浏览器的 `localStorage` 中。
    *   前端 `config.js` 在验证通过后，使用存储的 API Key 调用后端的 `/v1/config` API，获取完整的模型配置数据。
    *   `config.js` 将获取到的配置数据动态渲染到页面上，用户可以进行查看、修改、添加、删除等操作。
    *   当用户保存配置时，前端将更新后的完整配置 JSON 对象通过 `POST /v1/config` 发送给后端。
    *   后端的 `ModelManager` 接收到新配置后，会覆盖 `model_configs.json` 文件，并清空内部的模型实例缓存，以便下次请求时使用新配置重新创建实例。

## 4. 后端架构 (FastAPI)

后端是整个服务的核心，采用 FastAPI 构建，提供了异步、高性能的 API 服务。

### 模块划分

*   **`app/main.py`**:
    *   **作用**: FastAPI 应用的主入口。
    *   **职责**:
        *   创建和配置 FastAPI 实例，包括 CORS 中间件和静态文件服务。
        *   定义所有 API 端点（Endpoints），如 `/v1/chat/completions`, `/v1/models`, `/v1/config/*`。
        *   将业务逻辑委托给 `ModelManager`。
        *   处理全局异常和错误响应格式。

*   **`app/manager/model_manager.py`**:
    *   **作用**: 核心业务逻辑调度器。
    *   **职责**:
        *   加载、解析和验证 `model_configs.json` 文件。
        *   根据 API 请求中的模型名称，查找并提供相应的模型配置。
        *   管理模型实例的生命周期（创建和缓存），避免重复实例化。
        *   提供获取、更新、导入、导出配置的接口。
        *   将聊天请求分发给正确的模型实例（`DeepClaude` 或 `OpenAICompatibleComposite`）。

*   **`app/deepclaude/deepclaude.py`**:
    *   **作用**: 实现“DeepSeek + Claude”组合逻辑的类。
    *   **职责**:
        *   封装 `DeepSeekClient` 和 `ClaudeClient`。
        *   实现 `chat_completions_with_stream` 和 `chat_completions_without_stream` 方法。
        *   编排双模型调用流程：先调用 DeepSeek 获取推理，然后将推理结果与用户输入结合，再调用 Claude 获取最终答案。
        *   处理两个流式响应的衔接和数据格式转换。

*   **`app/openai_composite/openai_composite.py`**:
    *   **作用**: 实现“DeepSeek + 其他 OpenAI 兼容模型”组合逻辑的类。
    *   **职责**:
        *   与 `DeepClaude` 类似，但目标模型是通用的 OpenAI 兼容模型。
        *   封装 `DeepSeekClient` 和 `OpenAICompatibleClient`。
        *   处理与通用 OpenAI 兼容模型的交互。

*   **`app/clients/`**:
    *   **作用**: 外部 API 客户端的集合。
    *   **`base_client.py`**: (可能存在的基类，定义通用接口)
    *   **`deepseek_client.py`**: 封装与 DeepSeek API 的所有交互。
    *   **`claude_client.py`**: 封装与 Anthropic (Claude) API 的所有交互。
    *   **`openai_compatible_client.py`**: 封装与任何 OpenAI 兼容 API 的交互。

*   **`app/utils/`**:
    *   **`auth.py`**: 实现 API Key 验证的依赖函数 (`verify_api_key`)。
    *   **`logger.py`**: 提供全局的日志记录器实例。

*   **`app/model_manager/model_configs.json`**:
    *   **作用**: 项目的“大脑”，存储所有模型配置。
    *   **结构**:
        *   `reasoner_models`: 定义可用的推理模型（如 `deepseek-reasoner`）。
        *   `target_models`: 定义可用的目标模型（如 `claude-sonnet`, `gpt-4o`）。
        *   `composite_models`: 定义组合模型，将一个推理模型和一个目标模型绑定在一起，并赋予一个新的名称。这是客户端实际调用的模型。
        *   `proxy`: 全局代理设置。
        *   `system`: 系统级配置，如日志级别、主 API Key、CORS 设置等。

## 5. 前端架构 (Vanilla JS)

前端是一个单页面应用（SPA），用于配置后端服务。它没有使用任何现代前端框架，完全由原生 JavaScript、HTML 和 Bootstrap 5 构建。

### 文件结构

*   **`frontend/index.html`**:
    *   应用的唯一 HTML 文件，定义了页面的基本结构，包括授权页面和配置页面两个主要部分。
    *   引入了 Bootstrap 的 CSS/JS，以及自定义的 `styles.css`, `auth.js`, `config.js`, `main.js`。

*   **`frontend/css/styles.css`**:
    *   自定义的 CSS 样式，用于微调 Bootstrap 的默认外观。

*   **`frontend/js/main.js`**:
    *   **入口文件**。在 `DOMContentLoaded` 事件触发后，初始化 `Auth` 和 `Config` 模块。

*   **`frontend/js/auth.js`**:
    *   **身份验证模块**。
    *   **职责**:
        *   检查 `localStorage` 中是否存在 API Key。
        *   如果存在，则调用后端 API 进行验证。
        *   如果不存在或验证失败，则显示授权页面，要求用户输入 API Key。
        *   处理登录和登出逻辑，管理 API Key 在 `localStorage` 中的存取。

*   **`frontend/js/config.js`**:
    *   **配置管理模块**。
    *   **职责**:
        *   在身份验证成功后被激活。
        *   调用后端 `/v1/config` 接口获取完整的配置数据。
        *   动态地将配置数据渲染成 HTML 元素（如模型卡片、设置表单等）。
        *   处理用户在界面上的所有交互，如编辑、保存、添加、删除模型。
        *   将用户的修改更新到本地的 `configData` JavaScript 对象中。
        *   当用户点击“全部保存”时，将整个 `configData` 对象序列化为 JSON，并通过 `POST /v1/config` 发送给后端。
        *   处理配置的导入和导出功能。

## 6. 部署与运行

项目通过 Docker 和 Docker Compose 进行容器化，极大地简化了部署和环境管理。

*   **`Dockerfile`**:
    *   定义了如何构建后端 Python 应用的 Docker 镜像。
    *   使用多阶段构建（multi-stage build），先安装依赖，然后将应用代码复制到一个干净的 Python 运行时环境中，以减小最终镜像的体积。

*   **`docker-compose.yml`**:
    *   定义了如何运行整个服务。
    *   通常会包含一个 `app` 服务，该服务使用 `Dockerfile` 构建的镜像。
    *   负责端口映射（例如，将容器的 8000 端口映射到主机的某个端口）、卷挂载（如果需要持久化数据）以及网络配置。

*   **运行命令**:
    *   通过简单的 `docker-compose up -d --build` 命令，即可在后台构建并启动整个应用。